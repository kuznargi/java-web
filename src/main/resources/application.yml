spring:
  application:
    name: web

  datasource:
    url: jdbc:postgresql://localhost:5432/exam
    username: postgres
    password: 123

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  security:
    user:
      name: test
      password: test
